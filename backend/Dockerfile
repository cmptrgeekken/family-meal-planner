FROM node:22-alpine

WORKDIR /app

# Placeholder image for early infrastructure wiring.
# Replace with dependency installation and runtime commands once the API scaffold exists.
COPY . .

EXPOSE 3001

CMD ["node", "-e", "console.log('Backend scaffold not implemented yet.'); setInterval(() => {}, 1 << 30)"]
